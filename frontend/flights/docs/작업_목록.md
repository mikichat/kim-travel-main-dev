# 항공편 변환기 개선 작업 목록

## 📋 Phase 1: 이름 자동 변환 기능 (우선순위: 높음)

### 1.1 이름 파싱 함수 구현 ✅
- [x] `cleanPassengerName()` 함수 작성
  - 호칭(MR/MS/MSTR/MISS) 제거
  - 대소문자 구분 없이 처리
  - 앞뒤 공백 제거
  
- [x] `parsePassengerNames()` 함수 작성
  - 영문 형식: `1.KIM/YONGKUMR`, `1.1SO/JAEWANMR`
  - 한글 형식: `1.박/영근`, `1.권/형수 MR` ✅ (추가됨)
  - 승객 번호 순서대로 정렬
  - 중복 제거 로직 추가
  - 파싱된 이름 배열 반환

### 1.2 handleConvert() 함수 수정 ✅
- [x] 이름 파싱 로직 통합
  - `parsePassengerNames()` 호출
  - 파싱 결과 검증
  
- [x] 대표자 자동 설정
  - 첫 번째 이름을 `nameInput`에 자동 입력
  - 입력 이벤트 트리거
  
- [x] 총 인원수 자동 계산
  - 파싱된 이름 개수 계산
  - `totalPeopleInput`에 자동 입력
  - "명" 중복 방지

### 1.3 테스트 및 검증 ✅
- [x] 형식 1 테스트
  - 테스트 데이터 입력
  - 대표자 확인: "KIM/YONGKU" ✅
  - 총 인원 확인: "6명" ✅
  
- [x] 형식 2 테스트
  - 테스트 데이터 입력
  - 대표자 확인: "SO/JAEWAN" ✅
  - 총 인원 확인: "3명" ✅
  
- [x] 혼합 형식 테스트 (선택)
  - 두 형식이 섞인 데이터 테스트
  - 정상 작동 확인

---

## 📋 Phase 2: 저장 관리 기능 개선 (우선순위: 높음)

### 2.1 선택 삭제 기능 ✅
- [x] UI 추가 (`saved-flights.js`, `index.html`)
  - 각 항공편 카드에 체크박스 추가
  - 체크박스 스타일링
  - "선택 삭제" 버튼 추가
  
- [x] 삭제 로직 구현
  - `deleteSelectedFlights()` 함수 작성
  - 선택된 항목 ID 수집
  - 확인 대화상자 표시
  - `StorageManager.deleteFlightById()` 호출
  - 목록 새로고침

- [x] `flight-schedule.html` 버그 수정
  - `getSelectedFlights()`: `parseInt()` 제거 (문자열 ID 처리)
  - `deleteSelected()`: `FlightSyncManager.deleteFlight()` 연동

### 2.2 수정 저장 기능 ✅
- [x] 편집 모드 추적
  - `currentEditingFlightId` 전역 변수 추가
  - 불러오기 시 ID 저장
  
- [x] `flight-schedule.html` 버그 수정
  - `editFlight()`: passengers 객체 배열 처리 (이름만 추출)
  - `saveFlight()`: `flight.sourceId` 직접 사용으로 ID 추출 로직 간소화
  - `FlightSyncManager.updateFlight()` 정상 연동 확인

### 2.3 왕복 저장 오류 수정
- [ ] `parsedFlights` 배열 검증
  - 모든 항공편 구간 포함 확인
  - 디버그 로그 추가
  
- [ ] 저장 로직 수정
  - 모든 구간이 저장되도록 수정
  - 왕복/편도 구분 로직 확인
  
- [ ] 테스트
  - 왕복 항공편 저장 테스트
  - 저장된 데이터 확인
  - 불러오기 테스트

---

## 📋 Phase 3: 팀 통합 기능 (우선순위: 중간)

### 3.1 UI 추가
- [ ] "선택 통합" 버튼 추가
  - 버튼 위치: "선택 삭제" 버튼 옆
  - 버튼 스타일: 보라색 계열
  - 버튼 텍스트: "선택 통합 (팀 합치기)"

### 3.2 병합 로직 구현
- [ ] `mergeSelectedFlights()` 함수 작성
  - 선택된 항목 2개 이상 확인
  - 선택된 항공편 데이터 수집
  
- [ ] 데이터 통합
  - PNR: 쉼표로 구분하여 연결
  - 항공편: 모든 구간 합침 (`flatMap` 사용)
  - 탑승객 이름: 쉼표로 구분하여 연결
  - 총 인원: 합산
  - 이름: 첫 번째 항목 이름 + " (통합)"
  
- [ ] 저장 및 표시
  - `StorageManager.saveFlightData()` 호출
  - 성공 메시지 표시
  - 목록 새로고침

### 3.3 테스트 및 검증
- [ ] 2개 항목 통합 테스트
  - 서로 다른 PNR 선택
  - 통합 실행
  - 결과 확인
  
- [ ] 3개 이상 항목 통합 테스트
  - 여러 항목 선택
  - 통합 실행
  - 모든 데이터 포함 확인
  
- [ ] 엣지 케이스 테스트
  - 1개만 선택 시 경고 메시지
  - 빈 데이터 처리
  - 중복 데이터 처리

---

## 📋 Phase 4: 최종 검증 및 문서화

### 4.1 통합 테스트
- [ ] 전체 워크플로우 테스트
  - 형식 1 데이터 변환 → 저장 → 수정 → 삭제
  - 형식 2 데이터 변환 → 저장 → 통합
  - 혼합 시나리오 테스트
  
- [ ] 브라우저 호환성 테스트
  - Chrome 테스트
  - Edge 테스트
  - Firefox 테스트 (선택)

### 4.2 버그 수정
- [ ] 발견된 버그 목록 작성
- [ ] 우선순위별 버그 수정
- [ ] 수정 후 재테스트

### 4.3 문서 업데이트
- [ ] README 업데이트
  - 새로운 기능 설명 추가
  - 사용 방법 안내
  
- [ ] 변경 이력 작성
  - 버전 번호 업데이트
  - 주요 변경사항 기록
  
- [ ] 사용자 가이드 작성 (선택)
  - 스크린샷 추가
  - 단계별 사용 방법

---

## 📊 진행 상황 요약

| Phase | 작업 항목 | 상태 | 완료율 |
|-------|----------|------|--------|
| Phase 1 | 이름 자동 변환 | 🟢 완료 | 100% |
| Phase 2 | 저장 관리 개선 | 🟡 진행 중 | 66% |
| Phase 3 | 팀 통합 기능 | 🟡 진행 중 | 50% |
| Phase 4 | 최종 검증 | 🔴 대기 | 0% |

**범례:**
- 🔴 대기
- 🟡 진행 중
- 🟢 완료

---

## 🎯 우선순위 및 예상 시간

### 높음 (필수)
- Phase 1: 이름 자동 변환 - **30분**
- Phase 2: 저장 관리 개선 - **1시간**

### 중간 (권장)
- Phase 3: 팀 통합 기능 - **30분**

### 낮음 (선택)
- Phase 4: 최종 검증 - **30분**

**총 예상 시간: 2-2.5시간**

---

## 📝 참고 파일

### 수정할 파일
- `c:\Users\kgj12\Root\main\air1\js\main.js`
- `c:\Users\kgj12\Root\main\air1\js\saved-flights.js`
- `c:\Users\kgj12\Root\main\air1\js\storage-manager.js` (필요시)

### 참고 문서
- `c:\Users\kgj12\Root\main\air1\docs\항공편_변환기_개선_계획.md`
- `c:\Users\kgj12\Root\main\air1\docs\예약_형식_비교.md`

### 테스트 URL
- http://localhost:5000/air1/index.html
